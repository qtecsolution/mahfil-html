const videotype = NiceSelect.bind(document.getElementById("videotype"), {
  searchable: false,
  placeholder: 'Select video type'
});

const videostatus = NiceSelect.bind(document.getElementById("videostatus"), {
  searchable: false,
  placeholder: 'Select video status'
});

const publishingoption = NiceSelect.bind(document.getElementById("publishingoption"), {
  searchable: false,
  placeholder: 'Scheduled for later'
});